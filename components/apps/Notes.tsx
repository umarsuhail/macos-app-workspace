"use client";
import { useState } from "react";
import { Plus, Search } from "lucide-react";
const seed=["Project launch notes","Ideas for the client portal","September priorities"];
export default function Notes(){const [notes,setNotes]=useState(seed),[selected,setSelected]=useState(0);return <div className="notes-app"><aside><div className="notes-top"><strong>Notes</strong><button onClick={()=>{setNotes(["Untitled note",...notes]);setSelected(0)}}><Plus size={17}/></button></div><label><Search size={14}/><input placeholder="Search notes"/></label>{notes.map((n,i)=><button className={selected===i?"selected":""} key={`${n}-${i}`} onClick={()=>setSelected(i)}><strong>{n}</strong><small>{i===0?"Today":"This week"}</small></button>)}</aside><section><div><span>Edited just now</span><button>Done</button></div><input aria-label="Note title" value={notes[selected]} onChange={e=>setNotes(notes.map((n,i)=>i===selected?e.target.value:n))}/><textarea defaultValue={"A calm, fast workspace starts with a focused architecture.\n\n• Load each app only when it is opened\n• Keep application state inside the shell\n• Give every app its own error boundary\n• Let internal panels scroll, never the desktop\n\nThis note is editable — try it."}/></section></div>}
